const clamp = (value, low = 0, high = 1) => Math.max(low, Math.min(high, Number(value) || 0));
export const PREY_HYPOTHESIS_SCHEMA = 1;
export function createPreyHypothesis(observer, contact, options = {}) {
  const motion = contact.motionObservation || null, centre = motion?.position?.estimate || { x: contact.x, z: contact.z }, confidence = clamp(contact.confidence);
  return Object.freeze({ schemaVersion: PREY_HYPOTHESIS_SCHEMA, hypothesisId: `prey:${observer.id}:${contact.targetId}`, observerId: observer.id, subjectAssociationId: contact.targetId, classification: Object.freeze({ category: "prey", speciesEstimate: contact.identifiedSpecies || contact.speciesId || null, confidence }), locationRegion: Object.freeze({ centre: Object.freeze({ x: Number(centre.x), z: Number(centre.z) }), covariance: motion?.position?.covariance || Object.freeze({ xx: 1, xz: 0, zz: 1 }), confidence }), motion, vulnerability: Object.freeze({ apparentInjury: contact.bodyCues?.injury || null, apparentFatigue: contact.bodyCues?.fatigue || null, apparentIsolation: options.apparentIsolation ?? null, apparentYouthOrAge: contact.bodyCues?.lifeStage || null, escapeMarginEstimate: options.escapeMarginEstimate ?? null, confidence: contact.bodyCues ? confidence : confidence * .45, unknownFeatures: Object.freeze([...(options.unknownFeatures || [])]) }), opportunity: Object.freeze({ detectability: confidence, approachCover: clamp(options.approachCover), pursuitCostEstimate: Math.max(0, Number(options.pursuitCostEstimate || 0)), interceptionProbability: clamp(options.interceptionProbability ?? motion?.velocity?.confidence), competitionRisk: clamp(options.competitionRisk), confidence }), state: options.state || "observed", provenance: Object.freeze({ observationIds: Object.freeze([contact.evidenceId || contact.id].filter(Boolean)), memoryIds: Object.freeze([...(options.memoryIds || [])]), inferenceVersion: 1 }) });
}
