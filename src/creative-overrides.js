export function allowedCreativeOverrides(capabilities, requested = {}) { if (!capabilities?.creativeOverrides) return {}; return Object.fromEntries(Object.entries(requested).filter(([key, value]) => key.startsWith("disable") && value === true)); }
export function applyCreativeProtection(animal, before, overrides = {}) { if (overrides.disableHunger) { animal.stomach = Math.max(animal.stomach, before.stomach); animal.energy = Math.max(animal.energy, before.energy); } if (overrides.disableThirst) animal.hydration = Math.max(animal.hydration, before.hydration); if (overrides.disableDamage) { animal.health = Math.max(animal.health, before.health); animal.injuries = before.injuries || []; } if (overrides.disableAgeing) animal.age = before.age; if (overrides.disablePregnancyCosts && animal.pregnant) { animal.energy = Math.max(animal.energy, before.energy); animal.hydration = Math.max(animal.hydration, before.hydration); } if (overrides.disableDeath && !animal.alive) { animal.alive = true; animal.health = Math.max(1, animal.health || 0); delete animal.deathTick; } return animal; }
