import test from "node:test";
import assert from "node:assert/strict";
import { allocateAttention } from "../src/attention-controller.js";
import { degradeObservation, gradedObservation, VISION_PROFILE } from "../src/perception-observation.js";
test("central acuity exceeds peripheral while peripheral motion is detected", () => { const observer = { id: "g", speciesId: "grazer", x: 0, z: 0, orientation: 0, tick: 1 }; const central = gradedObservation(observer, { id: "h1", speciesId: "hunter", x: 3, z: 0, vx: .5, vz: 0 }, { visible: true, confidence: .8 }); const peripheral = gradedObservation(observer, { id: "h2", speciesId: "hunter", x: -2, z: 2, vx: .5, vz: 0 }, { visible: true, confidence: .8 }); assert.equal(central.region, "central"); assert.equal(peripheral.region, "peripheral"); assert.ok(central.confidence > peripheral.confidence); assert.equal(peripheral.detectedMotion, true); });
test("memory degrades dimensions independently and attention is bounded", () => { const faded = degradeObservation({ confidence: .9, velocityConfidence: .6, identifiedIndividual: "x", identifiedSpecies: "hunter", vx: 1, vz: 0 }, 7, VISION_PROFILE.grazer); assert.equal(faded.vx, null); assert.ok(faded.confidence > 0); assert.equal(faded.identifiedIndividual, null); const observations = Array.from({ length: 8 }, (_, i) => ({ targetId: String(i), confidence: .8, detectedMotion: true, identifiedSpecies: "grazer" })); assert.equal(allocateAttention({ speciesId: "hunter" }, observations).length, VISION_PROFILE.hunter.attentionCapacity); });
