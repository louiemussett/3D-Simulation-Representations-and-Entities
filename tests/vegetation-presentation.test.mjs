import test from "node:test";
import assert from "node:assert/strict";
import { vegetationPresentationState, VegetationPresentationInvalidator } from "../src/vegetation-presentation.js";
test("tiny grazing changes are coalesced until their visual band changes", () => { const tracker = new VegetationPresentationInvalidator(30), cell = { terrainClass: "grass", plantType: "grass", biomass: .55, grassHeight: .55 }; tracker.commit(1, vegetationPresentationState(cell)); cell.biomass = .54; cell.grassHeight = .54; assert.equal(tracker.observe(1, vegetationPresentationState(cell), 1), false); cell.biomass = .39; assert.equal(tracker.observe(1, vegetationPresentationState(cell), 2), true); });
test("sub-threshold changes receive a bounded delayed refresh", () => { const tracker = new VegetationPresentationInvalidator(30), cell = { terrainClass: "grass", plantType: "grass", biomass: .5, grassHeight: .5 }; tracker.commit(1, vegetationPresentationState(cell)); cell.biomass = .49; tracker.observe(1, vegetationPresentationState(cell), 5); assert.deepEqual(tracker.due(34), []); assert.deepEqual(tracker.due(35), [1]); });
test("structural vegetation changes invalidate immediately", () => { const tracker = new VegetationPresentationInvalidator(), cell = { plantType: "tree", woodland: true, biomass: 1 }; tracker.commit(1, vegetationPresentationState(cell)); cell.fallenTreeUntil = 100; assert.equal(tracker.observe(1, vegetationPresentationState(cell, 10), 10), true); });
